# schema — wire-format source of truth

**Empty until Phase 05, on purpose.**

The codec is hand-written in C and TypeScript first (Phase 02). Codegen from
`records.yaml` + `control.proto` arrives only after hand-written drift has
caused a real bug — that bug is the lesson (PROJECT.md sections 2 and 6).

Do not add generators here early.
