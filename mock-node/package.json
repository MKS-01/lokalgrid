{
  "name": "lokalgrid-mock-node",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Phase 00 — fake Lokalgrid node. Serves the real 32-byte track-record wire format over WebSocket so the Android app develops with the hardware unplugged.",
  "bin": {
    "lokalgrid-decode": "src/decode-cli.js"
  },
  "scripts": {
    "start": "node src/server.js",
    "golden": "node src/golden.js",
    "test": "node --test"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
